[package]
name = "spending_limit_account_contract"
authors = ["xochi-fi"]
compiler_version = ">=0.25.0"
type = "contract"

[dependencies]
aztec = { git = "https://github.com/AztecProtocol/aztec-packages", tag = "v4.2.0", directory = "noir-projects/aztec-nr/aztec" }
schnorr = { tag = "v0.2.0", git = "https://github.com/noir-lang/schnorr" }

# Tag must match the Aztec SDK version (currently 4.2.0).
# The schnorr dependency tracks the noir-lang/schnorr repo independently.
